<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme - Dark Mode forced -->
    <style name="Base.Theme.Aonime" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Color Scheme -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryContainer">@color/colorPrimaryVariant</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorOnSecondary">@color/colorOnSecondary</item>
        <item name="android:colorBackground">@color/colorBackground</item>
        <item name="colorSurface">@color/colorSurface</item>
        <item name="colorOnSurface">@color/colorOnSurface</item>
        <item name="colorError">@color/colorError</item>
        <!-- Window flags for full screen edge-to-edge -->
        <item name="android:statusBarColor">@color/bg_primary</item>
        <item name="android:navigationBarColor">@color/nav_bar_bg</item>
        <!-- Typography -->
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <style name="Theme.Aonime" parent="Base.Theme.Aonime" />

    <!-- Splash Screen Theme -->
    <style name="Theme.Aonime.Splash" parent="Theme.Aonime">
        <item name="android:windowBackground">@color/bg_splash</item>
        <item name="android:windowFullscreen">false</item>
    </style>

    <!-- Chip style for genre filter -->
    <style name="Widget.Aonime.FilterChip" parent="Widget.Material3.Chip.Filter">
        <item name="android:textColor">@color/text_secondary</item>
        <item name="chipBackgroundColor">@color/bg_card</item>
        <item name="chipStrokeColor">#2E2E50</item>
        <item name="chipStrokeWidth">1dp</item>
        <item name="checkedIconVisible">false</item>
    </style>

    <!-- Bottom Navigation style -->
    <style name="Widget.Aonime.BottomNav" parent="Widget.Material3.NavigationBar">
        <item name="android:background">@color/nav_bar_bg</item>
        <item name="itemIconTint">@drawable/nav_item_color</item>
        <item name="itemTextColor">@drawable/nav_item_color</item>
        <item name="itemActiveIndicatorStyle">@style/Widget.Aonime.BottomNav.ActiveIndicator</item>
        <item name="labelVisibilityMode">labeled</item>
    </style>

    <style name="Widget.Aonime.BottomNav.ActiveIndicator" parent="Widget.Material3.NavigationBar.ActiveIndicator">
        <item name="android:color">@color/ripple</item>
    </style>
</resources>