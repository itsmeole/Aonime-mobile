<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_primary">

    <!-- Browse Toolbar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:background="@color/bg_primary">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Browse"
            android:textColor="@color/text_primary"
            android:textSize="20sp"
            android:textStyle="bold"
            android:letterSpacing="0.04" />

    </LinearLayout>

    <!-- Search bar container -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:orientation="horizontal"
        android:background="@drawable/bg_search_bar"
        android:gravity="center_vertical"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="12dp"
        android:paddingStart="4dp"
        android:paddingEnd="14dp">

        <androidx.appcompat.widget.SearchView
            android:id="@+id/search_view"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:queryHint="@string/search_hint"
            app:iconifiedByDefault="false"
            app:searchIcon="@drawable/ic_search"
            android:background="@null" />

    </LinearLayout>

    <!-- Horizontal scrollable filter dropdowns -->
    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scrollbars="none"
        android:layout_marginBottom="12dp"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:clipToPadding="false">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- Type Filter -->
            <include layout="@layout/layout_filter_spinner" android:id="@+id/filter_type" />
            
            <View android:layout_width="8dp" android:layout_height="1dp" />

            <!-- Genre Filter -->
            <include layout="@layout/layout_filter_spinner" android:id="@+id/filter_genre" />

            <View android:layout_width="8dp" android:layout_height="1dp" />

            <!-- Status Filter -->
            <include layout="@layout/layout_filter_spinner" android:id="@+id/filter_status" />

            <View android:layout_width="8dp" android:layout_height="1dp" />

            <!-- Sort Filter -->
            <include layout="@layout/layout_filter_spinner" android:id="@+id/filter_sort" />

            <View android:layout_width="8dp" android:layout_height="1dp" />

            <!-- Season Filter -->
            <include layout="@layout/layout_filter_spinner" android:id="@+id/filter_season" />

            <View android:layout_width="8dp" android:layout_height="1dp" />

            <!-- Year Filter -->
            <include layout="@layout/layout_filter_spinner" android:id="@+id/filter_year" />

            <View android:layout_width="8dp" android:layout_height="1dp" />

            <!-- Rating Filter -->
            <include layout="@layout/layout_filter_spinner" android:id="@+id/filter_rating" />

            <View android:layout_width="8dp" android:layout_height="1dp" />

            <!-- Country Filter -->
            <include layout="@layout/layout_filter_spinner" android:id="@+id/filter_country" />

            <View android:layout_width="8dp" android:layout_height="1dp" />

            <!-- Language Filter -->
            <include layout="@layout/layout_filter_spinner" android:id="@+id/filter_language" />

        </LinearLayout>
    </HorizontalScrollView>

    <!-- Anime Grid -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_browse_grid"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:paddingBottom="8dp"
        android:clipToPadding="false"
        app:layoutManager="androidx.recyclerview.widget.GridLayoutManager"
        app:spanCount="2" />

</LinearLayout>
